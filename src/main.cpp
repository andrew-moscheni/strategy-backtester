#include <curl/curl.h>
#include <string.h>

//define globals locally here (DISABLE WHEN COMMITTING)
auto API_URL = "";
auto API_KEY = "";
auto API_SECRET_KEY = "";


int main(int argc, char* argv[]){

    return 0;
}